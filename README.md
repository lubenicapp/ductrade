# DucTrade

setup virtual environnement with python 3.8

install dependencies 
```bash
pip install -r requirements.txt
```

run 

```bash
python app.py <STOCK-NAME>

# example : python app.py TSLA
```


It will train a model on <STOCK-NAME> the first time called
or will load if exist
